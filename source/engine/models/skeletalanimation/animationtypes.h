#pragma once

// DIA associated the particle texture-animation enum with this PDB path.
// Keep the legacy include working without defining a second global enum.
#include "models/particles/jobs/particlestage.h"
