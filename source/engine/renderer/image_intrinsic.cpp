#include "imagemanager.h"

// Intrinsic image generation is owned by idImageManager on the PC path.  The
// separate translation unit and original filename are retained for build/PDB
// parity with the retail renderer.
