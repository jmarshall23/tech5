#include "parmstate.h"

// Retail split allocation/registration into this translation unit.  The PC
// implementation keeps the filename for PDB correlation; the definitions are
// centralized in parmstate.cpp so state initialization remains deterministic.
