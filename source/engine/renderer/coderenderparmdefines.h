#pragma once

#include "coderenderparm.h"

// Retail generated one global symbol per code parm.  The recovered Windows
// renderer registers the same 617 name/initializer pairs lazily, avoiding CRT
// initialization-order dependencies while retaining this generated path.
void RegisterRecoveredCodeRenderParms();
